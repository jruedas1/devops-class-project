<h1>Student#index</h1>
<% @students.each do | student | %>
<%= student.title + " , " + student.location + " , " + student.skills + " , " + student.summary + " , " + student.github + " , " + student.linkedin + " , " + student.school + " , " + student.photo %> 
<% end %>
